# Ruby C拡張によるベンチマーク比較

## 概要
rbc_globをRubyのC拡張として実装し、RubyのネイティブDir.globと同じ環境で性能を比較しました。

## 実装

### C拡張の構成
```
ext/rbcglob/
├── extconf.rb          # mkmf設定
├── rbcglob.c           # Ruby C API実装
└── rbcglob.so          # コンパイル済み拡張
```

### API
```ruby
RBCGlob.glob(pattern, flags: 0, base: nil) -> Array
```

Ruby Dir.globと同じインターフェースで、rbc_glob()をラップしています。

## ベンチマーク結果

### テスト環境
- **Ruby**: 4.0.0 (2025-12-25 revision 553f1675f3) +PRISM [aarch64-linux]
- **OS**: Ubuntu 24.04.2 LTS (Dev Container)
- **プロジェクト**: /workspaces/dirglob

### パターン別性能比較（マッチ数一致）

| パターン | Dir.glob (ms) | RBCGlob (ms) | 高速化 | マッチ数 |
|---------|--------------|-------------|-------|---------|
| 単純パターン (ルート) `*.c` | 0.138 | 0.117 | 1.18x | 0 |
| 再帰パターン (src) `src/**/*.c` | 0.199 | 0.186 | 1.07x | 5 |
| ブレース展開 (src) `src/**/*.{c,h}` | 0.213 | 0.195 | 1.09x | 8 |

**平均高速化**: 1.11x

### 全パターン結果（参考: マッチ数不一致含む）

| パターン | Dir.glob (ms) | RBCGlob (ms) | Dir.glob matches | RBCGlob matches |
|---------|--------------|-------------|-----------------|----------------|
| `*.c` | 0.138 | 0.117 | 0 | 0 |
| `**/*.c` | 34.066 | 0.716 | 147 | 9 |
| `src/**/*.c` | 0.199 | 0.186 | 5 | 5 |
| `tests/**/*.c` | 23.152 | 5.419 | 72 | 8 |
| `src/**/*.{c,h}` | 0.213 | 0.195 | 8 | 8 |
| `{src,tests,examples}/**/*.c` | 23.655 | 5.780 | 80 | 16 |

## 分析

### 性能特性

1. **小規模パターン**: 1.07x〜1.18x程度の改善
   - ファイル数が少ない場合、I/Oオーバーヘッドが支配的
   - トライ最適化の効果は限定的

2. **大規模パターン（参考値）**: 4x〜47x程度の大幅改善
   - `**/*.c`: 34.066ms → 0.716ms（47.6x）
   - `tests/**/*.c`: 23.152ms → 5.419ms（4.3x）
   - ただし**マッチ数が異なるため直接比較不可**

### 既知の問題

現在のrbc_glob実装には再帰パターンでのマッチ漏れがあります：

```
tests/**/*.c のマッチ数:
- Dir.glob: 72ファイル
- RBCGlob:   8ファイル

具体的な差分:
- tests/fixtures配下の64ファイルがマッチしていない
```

これはトライ実装のバグであり、修正が必要です。

## 前回のC直接ベンチマーク（BENCHMARK_RESULTS.md）との比較

| パターン | Ruby単体 (ms) | C直接 (ms) | Ruby拡張 (ms) |
|---------|--------------|-----------|-------------|
| `**/*.c` | 43.853 | 0.944 | 0.716 |

Ruby拡張版のほうがC直接実行より高速という予想外の結果が出ていますが、これは：
- マッチ数が異なる（Ruby単体: 146, C直接: 9, Ruby拡張: 9）
- 実装のバグにより正しく比較できていない

正確な比較のためには、マッチ漏れのバグ修正が必要です。

## 結論

### 成果
✅ RubyのC拡張としてrbc_globを正常にビルド・実行できた
✅ Ruby同一環境でのベンチマーク比較が可能になった
✅ 小規模パターンでは約1.1xの改善を確認

### 課題
❌ 再帰パターン（`**`）でマッチ漏れが発生
❌ `tests/**/*.c`が正しく動作せず、公平な比較ができない
❌ トライ実装の修正が必要

### 次のステップ
1. トライ実装の再帰パターン処理を修正
2. 全パターンでDir.globと完全一致することを確認
3. 再度ベンチマーク実行し、正確な性能差を測定
