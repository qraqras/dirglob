/*
 * Auto-generated test file for File.fnmatch compatibility
 * Generated by gen_c_fnmatch_tests.py from test_definitions.py
 * Total test cases: 418
 */

#include <unity.h>
#include <rbc/rbc.h>
#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void setUp(void) {
    /* Set up before each test */
}

void tearDown(void) {
    /* Clean up after each test */
}

/* ========================================================================
 * Test Functions
 * ======================================================================== */

void test_fm001(void)
{
    /* Escaped star matches literal star */
    const char *pattern = "\\*";
    const char *text = "*";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/fm001.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_fm002(void)
{
    /* NOESCAPE - backslash literal */
    const char *pattern = "\\*";
    const char *text = "\\*";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/fm002.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_fm003(void)
{
    /* Escaped backslash */
    const char *pattern = "\\\\";
    const char *text = "\\";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/fm003.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_fm010(void)
{
    /* Star no match dotfile (no DOTMATCH) */
    const char *pattern = "*";
    const char *text = ".hidden";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/fm010.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_fm011(void)
{
    /* Star matches dotfile (DOTMATCH) */
    const char *pattern = "*";
    const char *text = ".hidden";
    unsigned flags = RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/fm011.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_fm012(void)
{
    /* Explicit dot matches */
    const char *pattern = ".*";
    const char *text = ".hidden";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/fm012.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_fm020(void)
{
    /* Star matches slash (no PATHNAME) */
    const char *pattern = "*";
    const char *text = "a/b";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/fm020.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_fm021(void)
{
    /* Star no match slash (PATHNAME) */
    const char *pattern = "*";
    const char *text = "a/b";
    unsigned flags = RBC_FNM_PATHNAME;
    const char *expected_file = "tests/ruby_fnmatch_expected/fm021.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_fm022(void)
{
    /* Question no match slash (PATHNAME) */
    const char *pattern = "?";
    const char *text = "/";
    unsigned flags = RBC_FNM_PATHNAME;
    const char *expected_file = "tests/ruby_fnmatch_expected/fm022.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_fm030(void)
{
    /* Case sensitive - no match */
    const char *pattern = "test";
    const char *text = "TEST";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/fm030.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_fm031(void)
{
    /* Case insensitive match */
    const char *pattern = "test";
    const char *text = "TEST";
    unsigned flags = RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/fm031.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_fm032(void)
{
    /* Bracket with CASEFOLD */
    const char *pattern = "[a-z]";
    const char *text = "A";
    unsigned flags = RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/fm032.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_fm040(void)
{
    /* PATHNAME - dotfile no match */
    const char *pattern = "*/*.c";
    const char *text = "dir/.hidden.c";
    unsigned flags = RBC_FNM_PATHNAME;
    const char *expected_file = "tests/ruby_fnmatch_expected/fm040.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_fm041(void)
{
    /* PATHNAME + DOTMATCH */
    const char *pattern = "*/*.c";
    const char *text = "dir/.hidden.c";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/fm041.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1000(void)
{
    /* Pattern: '*.c', Text: 'test.c', Flags: NONE */
    const char *pattern = "*.c";
    const char *text = "test.c";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1000.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1001(void)
{
    /* Pattern: '*.c', Text: 'test.c', Flags: NOESCAPE */
    const char *pattern = "*.c";
    const char *text = "test.c";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1001.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1002(void)
{
    /* Pattern: '*.c', Text: 'test.c', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "*.c";
    const char *text = "test.c";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1002.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1003(void)
{
    /* Pattern: '*.c', Text: 'test.c', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "*.c";
    const char *text = "test.c";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1003.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1004(void)
{
    /* Pattern: '*.c', Text: 'test.h', Flags: NONE */
    const char *pattern = "*.c";
    const char *text = "test.h";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1004.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1005(void)
{
    /* Pattern: '*.c', Text: 'test.h', Flags: NOESCAPE */
    const char *pattern = "*.c";
    const char *text = "test.h";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1005.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1006(void)
{
    /* Pattern: '*.c', Text: 'test.h', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "*.c";
    const char *text = "test.h";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1006.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1007(void)
{
    /* Pattern: '*.c', Text: 'test.h', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "*.c";
    const char *text = "test.h";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1007.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1008(void)
{
    /* Pattern: '*.c', Text: '.hidden.c', Flags: NONE */
    const char *pattern = "*.c";
    const char *text = ".hidden.c";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1008.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1009(void)
{
    /* Pattern: '*.c', Text: '.hidden.c', Flags: DOTMATCH */
    const char *pattern = "*.c";
    const char *text = ".hidden.c";
    unsigned flags = RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1009.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1010(void)
{
    /* Pattern: '*.c', Text: '.hidden.c', Flags: NOESCAPE */
    const char *pattern = "*.c";
    const char *text = ".hidden.c";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1010.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1011(void)
{
    /* Pattern: '*.c', Text: '.hidden.c', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "*.c";
    const char *text = ".hidden.c";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1011.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1012(void)
{
    /* Pattern: '*.c', Text: '.hidden.c', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "*.c";
    const char *text = ".hidden.c";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1012.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1013(void)
{
    /* Pattern: '*.txt', Text: 'file.txt', Flags: NONE */
    const char *pattern = "*.txt";
    const char *text = "file.txt";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1013.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1014(void)
{
    /* Pattern: '*.txt', Text: 'file.txt', Flags: NOESCAPE */
    const char *pattern = "*.txt";
    const char *text = "file.txt";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1014.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1015(void)
{
    /* Pattern: '*.txt', Text: 'file.txt', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "*.txt";
    const char *text = "file.txt";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1015.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1016(void)
{
    /* Pattern: '*.txt', Text: 'file.txt', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "*.txt";
    const char *text = "file.txt";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1016.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1017(void)
{
    /* Pattern: '*.so', Text: 'libssl.so', Flags: NONE */
    const char *pattern = "*.so";
    const char *text = "libssl.so";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1017.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1018(void)
{
    /* Pattern: '*.so', Text: 'libssl.so', Flags: NOESCAPE */
    const char *pattern = "*.so";
    const char *text = "libssl.so";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1018.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1019(void)
{
    /* Pattern: '*.so', Text: 'libssl.so', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "*.so";
    const char *text = "libssl.so";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1019.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1020(void)
{
    /* Pattern: '*.so', Text: 'libssl.so', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "*.so";
    const char *text = "libssl.so";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1020.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1021(void)
{
    /* Pattern: 'test*', Text: 'test.c', Flags: NONE */
    const char *pattern = "test*";
    const char *text = "test.c";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1021.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1022(void)
{
    /* Pattern: 'test*', Text: 'test.c', Flags: NOESCAPE */
    const char *pattern = "test*";
    const char *text = "test.c";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1022.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1023(void)
{
    /* Pattern: 'test*', Text: 'test.c', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "test*";
    const char *text = "test.c";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1023.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1024(void)
{
    /* Pattern: 'test*', Text: 'test.c', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "test*";
    const char *text = "test.c";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1024.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1025(void)
{
    /* Pattern: 'test*', Text: 'testing', Flags: NONE */
    const char *pattern = "test*";
    const char *text = "testing";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1025.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1026(void)
{
    /* Pattern: 'test*', Text: 'testing', Flags: NOESCAPE */
    const char *pattern = "test*";
    const char *text = "testing";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1026.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1027(void)
{
    /* Pattern: 'test*', Text: 'testing', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "test*";
    const char *text = "testing";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1027.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1028(void)
{
    /* Pattern: 'test*', Text: 'testing', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "test*";
    const char *text = "testing";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1028.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1029(void)
{
    /* Pattern: 'lib*', Text: 'libssl.so', Flags: NONE */
    const char *pattern = "lib*";
    const char *text = "libssl.so";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1029.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1030(void)
{
    /* Pattern: 'lib*', Text: 'libssl.so', Flags: NOESCAPE */
    const char *pattern = "lib*";
    const char *text = "libssl.so";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1030.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1031(void)
{
    /* Pattern: 'lib*', Text: 'libssl.so', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "lib*";
    const char *text = "libssl.so";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1031.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1032(void)
{
    /* Pattern: 'lib*', Text: 'libssl.so', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "lib*";
    const char *text = "libssl.so";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1032.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1033(void)
{
    /* Pattern: '*', Text: '', Flags: NONE */
    const char *pattern = "*";
    const char *text = "";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1033.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1034(void)
{
    /* Pattern: '*', Text: '', Flags: NOESCAPE */
    const char *pattern = "*";
    const char *text = "";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1034.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1035(void)
{
    /* Pattern: '*', Text: '', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "*";
    const char *text = "";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1035.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1036(void)
{
    /* Pattern: '*', Text: '', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "*";
    const char *text = "";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1036.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1037(void)
{
    /* Pattern: '*', Text: 'anything', Flags: NONE */
    const char *pattern = "*";
    const char *text = "anything";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1037.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1038(void)
{
    /* Pattern: '*', Text: 'anything', Flags: NOESCAPE */
    const char *pattern = "*";
    const char *text = "anything";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1038.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1039(void)
{
    /* Pattern: '*', Text: 'anything', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "*";
    const char *text = "anything";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1039.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1040(void)
{
    /* Pattern: '*', Text: 'anything', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "*";
    const char *text = "anything";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1040.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1041(void)
{
    /* Pattern: '*', Text: 'a/b', Flags: NONE */
    const char *pattern = "*";
    const char *text = "a/b";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1041.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1042(void)
{
    /* Pattern: '*', Text: 'a/b', Flags: PATHNAME */
    const char *pattern = "*";
    const char *text = "a/b";
    unsigned flags = RBC_FNM_PATHNAME;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1042.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1043(void)
{
    /* Pattern: '*', Text: 'a/b', Flags: NOESCAPE */
    const char *pattern = "*";
    const char *text = "a/b";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1043.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1044(void)
{
    /* Pattern: '*', Text: 'a/b', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "*";
    const char *text = "a/b";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1044.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1045(void)
{
    /* Pattern: '*', Text: 'a/b', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "*";
    const char *text = "a/b";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1045.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1046(void)
{
    /* Pattern: '*', Text: '.hidden', Flags: NONE */
    const char *pattern = "*";
    const char *text = ".hidden";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1046.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1047(void)
{
    /* Pattern: '*', Text: '.hidden', Flags: DOTMATCH */
    const char *pattern = "*";
    const char *text = ".hidden";
    unsigned flags = RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1047.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1048(void)
{
    /* Pattern: '*', Text: '.hidden', Flags: NOESCAPE */
    const char *pattern = "*";
    const char *text = ".hidden";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1048.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1049(void)
{
    /* Pattern: '*', Text: '.hidden', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "*";
    const char *text = ".hidden";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1049.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1050(void)
{
    /* Pattern: '*', Text: '.hidden', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "*";
    const char *text = ".hidden";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1050.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1051(void)
{
    /* Pattern: 'a*c', Text: 'abc', Flags: NONE */
    const char *pattern = "a*c";
    const char *text = "abc";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1051.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1052(void)
{
    /* Pattern: 'a*c', Text: 'abc', Flags: NOESCAPE */
    const char *pattern = "a*c";
    const char *text = "abc";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1052.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1053(void)
{
    /* Pattern: 'a*c', Text: 'abc', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "a*c";
    const char *text = "abc";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1053.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1054(void)
{
    /* Pattern: 'a*c', Text: 'abc', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "a*c";
    const char *text = "abc";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1054.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1055(void)
{
    /* Pattern: 'a*c', Text: 'axyzc', Flags: NONE */
    const char *pattern = "a*c";
    const char *text = "axyzc";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1055.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1056(void)
{
    /* Pattern: 'a*c', Text: 'axyzc', Flags: NOESCAPE */
    const char *pattern = "a*c";
    const char *text = "axyzc";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1056.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1057(void)
{
    /* Pattern: 'a*c', Text: 'axyzc', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "a*c";
    const char *text = "axyzc";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1057.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1058(void)
{
    /* Pattern: 'a*c', Text: 'axyzc', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "a*c";
    const char *text = "axyzc";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1058.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1059(void)
{
    /* Pattern: 'a*c', Text: 'adc', Flags: NONE */
    const char *pattern = "a*c";
    const char *text = "adc";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1059.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1060(void)
{
    /* Pattern: 'a*c', Text: 'adc', Flags: NOESCAPE */
    const char *pattern = "a*c";
    const char *text = "adc";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1060.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1061(void)
{
    /* Pattern: 'a*c', Text: 'adc', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "a*c";
    const char *text = "adc";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1061.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1062(void)
{
    /* Pattern: 'a*c', Text: 'adc', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "a*c";
    const char *text = "adc";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1062.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1063(void)
{
    /* Pattern: '*test*', Text: 'testing', Flags: NONE */
    const char *pattern = "*test*";
    const char *text = "testing";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1063.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1064(void)
{
    /* Pattern: '*test*', Text: 'testing', Flags: NOESCAPE */
    const char *pattern = "*test*";
    const char *text = "testing";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1064.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1065(void)
{
    /* Pattern: '*test*', Text: 'testing', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "*test*";
    const char *text = "testing";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1065.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1066(void)
{
    /* Pattern: '*test*', Text: 'testing', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "*test*";
    const char *text = "testing";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1066.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1067(void)
{
    /* Pattern: '*test*', Text: 'atestb', Flags: NONE */
    const char *pattern = "*test*";
    const char *text = "atestb";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1067.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1068(void)
{
    /* Pattern: '*test*', Text: 'atestb', Flags: NOESCAPE */
    const char *pattern = "*test*";
    const char *text = "atestb";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1068.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1069(void)
{
    /* Pattern: '*test*', Text: 'atestb', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "*test*";
    const char *text = "atestb";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1069.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1070(void)
{
    /* Pattern: '*test*', Text: 'atestb', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "*test*";
    const char *text = "atestb";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1070.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1071(void)
{
    /* Pattern: '*test*', Text: 'test', Flags: NONE */
    const char *pattern = "*test*";
    const char *text = "test";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1071.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1072(void)
{
    /* Pattern: '*test*', Text: 'test', Flags: NOESCAPE */
    const char *pattern = "*test*";
    const char *text = "test";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1072.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1073(void)
{
    /* Pattern: '*test*', Text: 'test', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "*test*";
    const char *text = "test";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1073.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1074(void)
{
    /* Pattern: '*test*', Text: 'test', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "*test*";
    const char *text = "test";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1074.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1075(void)
{
    /* Pattern: '**', Text: 'test', Flags: NONE */
    const char *pattern = "**";
    const char *text = "test";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1075.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1076(void)
{
    /* Pattern: '**', Text: 'test', Flags: NOESCAPE */
    const char *pattern = "**";
    const char *text = "test";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1076.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1077(void)
{
    /* Pattern: '**', Text: 'test', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "**";
    const char *text = "test";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1077.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1078(void)
{
    /* Pattern: '**', Text: 'test', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "**";
    const char *text = "test";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1078.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1079(void)
{
    /* Pattern: '***', Text: 'test', Flags: NONE */
    const char *pattern = "***";
    const char *text = "test";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1079.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1080(void)
{
    /* Pattern: '***', Text: 'test', Flags: NOESCAPE */
    const char *pattern = "***";
    const char *text = "test";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1080.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1081(void)
{
    /* Pattern: '***', Text: 'test', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "***";
    const char *text = "test";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1081.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1082(void)
{
    /* Pattern: '***', Text: 'test', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "***";
    const char *text = "test";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1082.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1083(void)
{
    /* Pattern: '****c', Text: 'test.c', Flags: NONE */
    const char *pattern = "****c";
    const char *text = "test.c";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1083.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1084(void)
{
    /* Pattern: '****c', Text: 'test.c', Flags: NOESCAPE */
    const char *pattern = "****c";
    const char *text = "test.c";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1084.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1085(void)
{
    /* Pattern: '****c', Text: 'test.c', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "****c";
    const char *text = "test.c";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1085.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1086(void)
{
    /* Pattern: '****c', Text: 'test.c', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "****c";
    const char *text = "test.c";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1086.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1087(void)
{
    /* Pattern: 'test_*.c', Text: 'test_main.c', Flags: NONE */
    const char *pattern = "test_*.c";
    const char *text = "test_main.c";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1087.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1088(void)
{
    /* Pattern: 'test_*.c', Text: 'test_main.c', Flags: NOESCAPE */
    const char *pattern = "test_*.c";
    const char *text = "test_main.c";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1088.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1089(void)
{
    /* Pattern: 'test_*.c', Text: 'test_main.c', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "test_*.c";
    const char *text = "test_main.c";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1089.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1090(void)
{
    /* Pattern: 'test_*.c', Text: 'test_main.c', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "test_*.c";
    const char *text = "test_main.c";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1090.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1091(void)
{
    /* Pattern: 'lib*.so', Text: 'libssl.so', Flags: NONE */
    const char *pattern = "lib*.so";
    const char *text = "libssl.so";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1091.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1092(void)
{
    /* Pattern: 'lib*.so', Text: 'libssl.so', Flags: NOESCAPE */
    const char *pattern = "lib*.so";
    const char *text = "libssl.so";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1092.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1093(void)
{
    /* Pattern: 'lib*.so', Text: 'libssl.so', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "lib*.so";
    const char *text = "libssl.so";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1093.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1094(void)
{
    /* Pattern: 'lib*.so', Text: 'libssl.so', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "lib*.so";
    const char *text = "libssl.so";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1094.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1095(void)
{
    /* Pattern: '*.so.1.0', Text: 'libssl.so.1.0', Flags: NONE */
    const char *pattern = "*.so.1.0";
    const char *text = "libssl.so.1.0";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1095.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1096(void)
{
    /* Pattern: '*.so.1.0', Text: 'libssl.so.1.0', Flags: NOESCAPE */
    const char *pattern = "*.so.1.0";
    const char *text = "libssl.so.1.0";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1096.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1097(void)
{
    /* Pattern: '*.so.1.0', Text: 'libssl.so.1.0', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "*.so.1.0";
    const char *text = "libssl.so.1.0";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1097.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1098(void)
{
    /* Pattern: '*.so.1.0', Text: 'libssl.so.1.0', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "*.so.1.0";
    const char *text = "libssl.so.1.0";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1098.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1099(void)
{
    /* Pattern: '?', Text: 'a', Flags: NONE */
    const char *pattern = "?";
    const char *text = "a";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1099.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1100(void)
{
    /* Pattern: '?', Text: 'a', Flags: NOESCAPE */
    const char *pattern = "?";
    const char *text = "a";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1100.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1101(void)
{
    /* Pattern: '?', Text: 'a', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "?";
    const char *text = "a";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1101.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1102(void)
{
    /* Pattern: '?', Text: 'a', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "?";
    const char *text = "a";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1102.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1103(void)
{
    /* Pattern: '?', Text: '', Flags: NONE */
    const char *pattern = "?";
    const char *text = "";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1103.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1104(void)
{
    /* Pattern: '?', Text: '', Flags: NOESCAPE */
    const char *pattern = "?";
    const char *text = "";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1104.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1105(void)
{
    /* Pattern: '?', Text: '', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "?";
    const char *text = "";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1105.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1106(void)
{
    /* Pattern: '?', Text: '', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "?";
    const char *text = "";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1106.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1107(void)
{
    /* Pattern: '?', Text: 'ab', Flags: NONE */
    const char *pattern = "?";
    const char *text = "ab";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1107.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1108(void)
{
    /* Pattern: '?', Text: 'ab', Flags: NOESCAPE */
    const char *pattern = "?";
    const char *text = "ab";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1108.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1109(void)
{
    /* Pattern: '?', Text: 'ab', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "?";
    const char *text = "ab";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1109.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1110(void)
{
    /* Pattern: '?', Text: 'ab', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "?";
    const char *text = "ab";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1110.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1111(void)
{
    /* Pattern: '??', Text: 'ab', Flags: NONE */
    const char *pattern = "??";
    const char *text = "ab";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1111.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1112(void)
{
    /* Pattern: '??', Text: 'ab', Flags: NOESCAPE */
    const char *pattern = "??";
    const char *text = "ab";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1112.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1113(void)
{
    /* Pattern: '??', Text: 'ab', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "??";
    const char *text = "ab";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1113.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1114(void)
{
    /* Pattern: '??', Text: 'ab', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "??";
    const char *text = "ab";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1114.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1115(void)
{
    /* Pattern: '??', Text: 'a', Flags: NONE */
    const char *pattern = "??";
    const char *text = "a";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1115.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1116(void)
{
    /* Pattern: '??', Text: 'a', Flags: NOESCAPE */
    const char *pattern = "??";
    const char *text = "a";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1116.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1117(void)
{
    /* Pattern: '??', Text: 'a', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "??";
    const char *text = "a";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1117.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1118(void)
{
    /* Pattern: '??', Text: 'a', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "??";
    const char *text = "a";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1118.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1119(void)
{
    /* Pattern: '???', Text: 'abc', Flags: NONE */
    const char *pattern = "???";
    const char *text = "abc";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1119.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1120(void)
{
    /* Pattern: '???', Text: 'abc', Flags: NOESCAPE */
    const char *pattern = "???";
    const char *text = "abc";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1120.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1121(void)
{
    /* Pattern: '???', Text: 'abc', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "???";
    const char *text = "abc";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1121.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1122(void)
{
    /* Pattern: '???', Text: 'abc', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "???";
    const char *text = "abc";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1122.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1123(void)
{
    /* Pattern: '????.c', Text: 'test.c', Flags: NONE */
    const char *pattern = "????.c";
    const char *text = "test.c";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1123.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1124(void)
{
    /* Pattern: '????.c', Text: 'test.c', Flags: NOESCAPE */
    const char *pattern = "????.c";
    const char *text = "test.c";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1124.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1125(void)
{
    /* Pattern: '????.c', Text: 'test.c', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "????.c";
    const char *text = "test.c";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1125.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1126(void)
{
    /* Pattern: '????.c', Text: 'test.c', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "????.c";
    const char *text = "test.c";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1126.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1127(void)
{
    /* Pattern: '[abc]', Text: 'a', Flags: NONE */
    const char *pattern = "[abc]";
    const char *text = "a";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1127.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1128(void)
{
    /* Pattern: '[abc]', Text: 'a', Flags: NOESCAPE */
    const char *pattern = "[abc]";
    const char *text = "a";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1128.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1129(void)
{
    /* Pattern: '[abc]', Text: 'a', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "[abc]";
    const char *text = "a";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1129.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1130(void)
{
    /* Pattern: '[abc]', Text: 'a', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "[abc]";
    const char *text = "a";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1130.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1131(void)
{
    /* Pattern: '[abc]', Text: 'b', Flags: NONE */
    const char *pattern = "[abc]";
    const char *text = "b";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1131.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1132(void)
{
    /* Pattern: '[abc]', Text: 'b', Flags: NOESCAPE */
    const char *pattern = "[abc]";
    const char *text = "b";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1132.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1133(void)
{
    /* Pattern: '[abc]', Text: 'b', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "[abc]";
    const char *text = "b";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1133.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1134(void)
{
    /* Pattern: '[abc]', Text: 'b', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "[abc]";
    const char *text = "b";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1134.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1135(void)
{
    /* Pattern: '[abc]', Text: 'c', Flags: NONE */
    const char *pattern = "[abc]";
    const char *text = "c";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1135.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1136(void)
{
    /* Pattern: '[abc]', Text: 'c', Flags: NOESCAPE */
    const char *pattern = "[abc]";
    const char *text = "c";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1136.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1137(void)
{
    /* Pattern: '[abc]', Text: 'c', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "[abc]";
    const char *text = "c";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1137.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1138(void)
{
    /* Pattern: '[abc]', Text: 'c', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "[abc]";
    const char *text = "c";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1138.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1139(void)
{
    /* Pattern: '[abc]', Text: 'd', Flags: NONE */
    const char *pattern = "[abc]";
    const char *text = "d";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1139.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1140(void)
{
    /* Pattern: '[abc]', Text: 'd', Flags: NOESCAPE */
    const char *pattern = "[abc]";
    const char *text = "d";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1140.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1141(void)
{
    /* Pattern: '[abc]', Text: 'd', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "[abc]";
    const char *text = "d";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1141.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1142(void)
{
    /* Pattern: '[abc]', Text: 'd', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "[abc]";
    const char *text = "d";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1142.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1143(void)
{
    /* Pattern: '[abc]', Text: '', Flags: NONE */
    const char *pattern = "[abc]";
    const char *text = "";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1143.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1144(void)
{
    /* Pattern: '[abc]', Text: '', Flags: NOESCAPE */
    const char *pattern = "[abc]";
    const char *text = "";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1144.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1145(void)
{
    /* Pattern: '[abc]', Text: '', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "[abc]";
    const char *text = "";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1145.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1146(void)
{
    /* Pattern: '[abc]', Text: '', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "[abc]";
    const char *text = "";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1146.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1147(void)
{
    /* Pattern: '[a-z]', Text: 'a', Flags: NONE */
    const char *pattern = "[a-z]";
    const char *text = "a";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1147.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1148(void)
{
    /* Pattern: '[a-z]', Text: 'a', Flags: NOESCAPE */
    const char *pattern = "[a-z]";
    const char *text = "a";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1148.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1149(void)
{
    /* Pattern: '[a-z]', Text: 'a', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "[a-z]";
    const char *text = "a";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1149.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1150(void)
{
    /* Pattern: '[a-z]', Text: 'a', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "[a-z]";
    const char *text = "a";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1150.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1151(void)
{
    /* Pattern: '[a-z]', Text: 'm', Flags: NONE */
    const char *pattern = "[a-z]";
    const char *text = "m";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1151.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1152(void)
{
    /* Pattern: '[a-z]', Text: 'm', Flags: NOESCAPE */
    const char *pattern = "[a-z]";
    const char *text = "m";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1152.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1153(void)
{
    /* Pattern: '[a-z]', Text: 'm', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "[a-z]";
    const char *text = "m";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1153.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1154(void)
{
    /* Pattern: '[a-z]', Text: 'm', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "[a-z]";
    const char *text = "m";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1154.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1155(void)
{
    /* Pattern: '[a-z]', Text: 'z', Flags: NONE */
    const char *pattern = "[a-z]";
    const char *text = "z";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1155.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1156(void)
{
    /* Pattern: '[a-z]', Text: 'z', Flags: NOESCAPE */
    const char *pattern = "[a-z]";
    const char *text = "z";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1156.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1157(void)
{
    /* Pattern: '[a-z]', Text: 'z', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "[a-z]";
    const char *text = "z";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1157.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1158(void)
{
    /* Pattern: '[a-z]', Text: 'z', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "[a-z]";
    const char *text = "z";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1158.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1159(void)
{
    /* Pattern: '[a-z]', Text: 'A', Flags: NONE */
    const char *pattern = "[a-z]";
    const char *text = "A";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1159.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1160(void)
{
    /* Pattern: '[a-z]', Text: 'A', Flags: CASEFOLD */
    const char *pattern = "[a-z]";
    const char *text = "A";
    unsigned flags = RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1160.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1161(void)
{
    /* Pattern: '[a-z]', Text: 'A', Flags: NOESCAPE */
    const char *pattern = "[a-z]";
    const char *text = "A";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1161.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1162(void)
{
    /* Pattern: '[a-z]', Text: 'A', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "[a-z]";
    const char *text = "A";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1162.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1163(void)
{
    /* Pattern: '[a-z]', Text: 'A', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "[a-z]";
    const char *text = "A";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1163.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1164(void)
{
    /* Pattern: '[0-9]', Text: '5', Flags: NONE */
    const char *pattern = "[0-9]";
    const char *text = "5";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1164.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1165(void)
{
    /* Pattern: '[0-9]', Text: '5', Flags: NOESCAPE */
    const char *pattern = "[0-9]";
    const char *text = "5";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1165.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1166(void)
{
    /* Pattern: '[0-9]', Text: '5', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "[0-9]";
    const char *text = "5";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1166.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1167(void)
{
    /* Pattern: '[0-9]', Text: '5', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "[0-9]";
    const char *text = "5";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1167.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1168(void)
{
    /* Pattern: '[0-9]', Text: 'a', Flags: NONE */
    const char *pattern = "[0-9]";
    const char *text = "a";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1168.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1169(void)
{
    /* Pattern: '[0-9]', Text: 'a', Flags: NOESCAPE */
    const char *pattern = "[0-9]";
    const char *text = "a";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1169.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1170(void)
{
    /* Pattern: '[0-9]', Text: 'a', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "[0-9]";
    const char *text = "a";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1170.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1171(void)
{
    /* Pattern: '[0-9]', Text: 'a', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "[0-9]";
    const char *text = "a";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1171.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1172(void)
{
    /* Pattern: '[!abc]', Text: 'a', Flags: NONE */
    const char *pattern = "[!abc]";
    const char *text = "a";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1172.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1173(void)
{
    /* Pattern: '[!abc]', Text: 'a', Flags: NOESCAPE */
    const char *pattern = "[!abc]";
    const char *text = "a";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1173.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1174(void)
{
    /* Pattern: '[!abc]', Text: 'a', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "[!abc]";
    const char *text = "a";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1174.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1175(void)
{
    /* Pattern: '[!abc]', Text: 'a', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "[!abc]";
    const char *text = "a";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1175.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1176(void)
{
    /* Pattern: '[!abc]', Text: 'd', Flags: NONE */
    const char *pattern = "[!abc]";
    const char *text = "d";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1176.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1177(void)
{
    /* Pattern: '[!abc]', Text: 'd', Flags: NOESCAPE */
    const char *pattern = "[!abc]";
    const char *text = "d";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1177.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1178(void)
{
    /* Pattern: '[!abc]', Text: 'd', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "[!abc]";
    const char *text = "d";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1178.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1179(void)
{
    /* Pattern: '[!abc]', Text: 'd', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "[!abc]";
    const char *text = "d";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1179.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1180(void)
{
    /* Pattern: '[^a-z]', Text: 'a', Flags: NONE */
    const char *pattern = "[^a-z]";
    const char *text = "a";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1180.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1181(void)
{
    /* Pattern: '[^a-z]', Text: 'a', Flags: NOESCAPE */
    const char *pattern = "[^a-z]";
    const char *text = "a";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1181.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1182(void)
{
    /* Pattern: '[^a-z]', Text: 'a', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "[^a-z]";
    const char *text = "a";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1182.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1183(void)
{
    /* Pattern: '[^a-z]', Text: 'a', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "[^a-z]";
    const char *text = "a";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1183.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1184(void)
{
    /* Pattern: '[^a-z]', Text: '1', Flags: NONE */
    const char *pattern = "[^a-z]";
    const char *text = "1";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1184.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1185(void)
{
    /* Pattern: '[^a-z]', Text: '1', Flags: NOESCAPE */
    const char *pattern = "[^a-z]";
    const char *text = "1";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1185.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1186(void)
{
    /* Pattern: '[^a-z]', Text: '1', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "[^a-z]";
    const char *text = "1";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1186.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1187(void)
{
    /* Pattern: '[^a-z]', Text: '1', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "[^a-z]";
    const char *text = "1";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1187.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1188(void)
{
    /* Pattern: 'file[0-9].txt', Text: 'file1.txt', Flags: NONE */
    const char *pattern = "file[0-9].txt";
    const char *text = "file1.txt";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1188.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1189(void)
{
    /* Pattern: 'file[0-9].txt', Text: 'file1.txt', Flags: NOESCAPE */
    const char *pattern = "file[0-9].txt";
    const char *text = "file1.txt";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1189.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1190(void)
{
    /* Pattern: 'file[0-9].txt', Text: 'file1.txt', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "file[0-9].txt";
    const char *text = "file1.txt";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1190.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1191(void)
{
    /* Pattern: 'file[0-9].txt', Text: 'file1.txt', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "file[0-9].txt";
    const char *text = "file1.txt";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1191.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1192(void)
{
    /* Pattern: 'file[0-9].txt', Text: 'file5.txt', Flags: NONE */
    const char *pattern = "file[0-9].txt";
    const char *text = "file5.txt";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1192.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1193(void)
{
    /* Pattern: 'file[0-9].txt', Text: 'file5.txt', Flags: NOESCAPE */
    const char *pattern = "file[0-9].txt";
    const char *text = "file5.txt";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1193.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1194(void)
{
    /* Pattern: 'file[0-9].txt', Text: 'file5.txt', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "file[0-9].txt";
    const char *text = "file5.txt";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1194.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1195(void)
{
    /* Pattern: 'file[0-9].txt', Text: 'file5.txt', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "file[0-9].txt";
    const char *text = "file5.txt";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1195.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1196(void)
{
    /* Pattern: 'file[0-9].txt', Text: 'filea.txt', Flags: NONE */
    const char *pattern = "file[0-9].txt";
    const char *text = "filea.txt";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1196.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1197(void)
{
    /* Pattern: 'file[0-9].txt', Text: 'filea.txt', Flags: NOESCAPE */
    const char *pattern = "file[0-9].txt";
    const char *text = "filea.txt";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1197.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1198(void)
{
    /* Pattern: 'file[0-9].txt', Text: 'filea.txt', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "file[0-9].txt";
    const char *text = "filea.txt";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1198.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1199(void)
{
    /* Pattern: 'file[0-9].txt', Text: 'filea.txt', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "file[0-9].txt";
    const char *text = "filea.txt";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1199.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1200(void)
{
    /* Pattern: '[a-z][0-9]', Text: 'a1', Flags: NONE */
    const char *pattern = "[a-z][0-9]";
    const char *text = "a1";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1200.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1201(void)
{
    /* Pattern: '[a-z][0-9]', Text: 'a1', Flags: NOESCAPE */
    const char *pattern = "[a-z][0-9]";
    const char *text = "a1";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1201.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1202(void)
{
    /* Pattern: '[a-z][0-9]', Text: 'a1', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "[a-z][0-9]";
    const char *text = "a1";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1202.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1203(void)
{
    /* Pattern: '[a-z][0-9]', Text: 'a1', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "[a-z][0-9]";
    const char *text = "a1";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1203.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1204(void)
{
    /* Pattern: '[a-z][0-9]', Text: 'z9', Flags: NONE */
    const char *pattern = "[a-z][0-9]";
    const char *text = "z9";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1204.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1205(void)
{
    /* Pattern: '[a-z][0-9]', Text: 'z9', Flags: NOESCAPE */
    const char *pattern = "[a-z][0-9]";
    const char *text = "z9";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1205.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1206(void)
{
    /* Pattern: '[a-z][0-9]', Text: 'z9', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "[a-z][0-9]";
    const char *text = "z9";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1206.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1207(void)
{
    /* Pattern: '[a-z][0-9]', Text: 'z9', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "[a-z][0-9]";
    const char *text = "z9";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1207.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1208(void)
{
    /* Pattern: '\*', Text: '*', Flags: NONE */
    const char *pattern = "\\*";
    const char *text = "*";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1208.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1209(void)
{
    /* Pattern: '\*', Text: '*', Flags: NOESCAPE */
    const char *pattern = "\\*";
    const char *text = "*";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1209.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1210(void)
{
    /* Pattern: '\*', Text: 'test', Flags: NONE */
    const char *pattern = "\\*";
    const char *text = "test";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1210.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1211(void)
{
    /* Pattern: '\*', Text: 'test', Flags: NOESCAPE */
    const char *pattern = "\\*";
    const char *text = "test";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1211.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1212(void)
{
    /* Pattern: '\?', Text: '?', Flags: NONE */
    const char *pattern = "\\?";
    const char *text = "?";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1212.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1213(void)
{
    /* Pattern: '\?', Text: '?', Flags: NOESCAPE */
    const char *pattern = "\\?";
    const char *text = "?";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1213.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1214(void)
{
    /* Pattern: '\[', Text: '[', Flags: NONE */
    const char *pattern = "\\[";
    const char *text = "[";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1214.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1215(void)
{
    /* Pattern: '\[', Text: '[', Flags: NOESCAPE */
    const char *pattern = "\\[";
    const char *text = "[";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1215.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1216(void)
{
    /* Pattern: '\\', Text: '\', Flags: NONE */
    const char *pattern = "\\\\";
    const char *text = "\\";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1216.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1217(void)
{
    /* Pattern: '\\', Text: '\', Flags: NOESCAPE */
    const char *pattern = "\\\\";
    const char *text = "\\";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1217.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1218(void)
{
    /* Pattern: 'test\*.c', Text: 'test*.c', Flags: NONE */
    const char *pattern = "test\\*.c";
    const char *text = "test*.c";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1218.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1219(void)
{
    /* Pattern: 'test\*.c', Text: 'test*.c', Flags: NOESCAPE */
    const char *pattern = "test\\*.c";
    const char *text = "test*.c";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1219.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1220(void)
{
    /* Pattern: 'a/b', Text: 'a/b', Flags: NONE */
    const char *pattern = "a/b";
    const char *text = "a/b";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1220.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1221(void)
{
    /* Pattern: 'a/b', Text: 'a/b', Flags: PATHNAME */
    const char *pattern = "a/b";
    const char *text = "a/b";
    unsigned flags = RBC_FNM_PATHNAME;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1221.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1222(void)
{
    /* Pattern: 'a/b', Text: 'a/b', Flags: NOESCAPE */
    const char *pattern = "a/b";
    const char *text = "a/b";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1222.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1223(void)
{
    /* Pattern: 'a/b', Text: 'a/b', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "a/b";
    const char *text = "a/b";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1223.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1224(void)
{
    /* Pattern: 'a/b', Text: 'a/b', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "a/b";
    const char *text = "a/b";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1224.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1225(void)
{
    /* Pattern: 'a/b', Text: 'a/c', Flags: NONE */
    const char *pattern = "a/b";
    const char *text = "a/c";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1225.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1226(void)
{
    /* Pattern: 'a/b', Text: 'a/c', Flags: PATHNAME */
    const char *pattern = "a/b";
    const char *text = "a/c";
    unsigned flags = RBC_FNM_PATHNAME;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1226.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1227(void)
{
    /* Pattern: 'a/b', Text: 'a/c', Flags: NOESCAPE */
    const char *pattern = "a/b";
    const char *text = "a/c";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1227.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1228(void)
{
    /* Pattern: 'a/b', Text: 'a/c', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "a/b";
    const char *text = "a/c";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1228.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1229(void)
{
    /* Pattern: 'a/b', Text: 'a/c', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "a/b";
    const char *text = "a/c";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1229.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1230(void)
{
    /* Pattern: '* /*', Text: 'a/b', Flags: NONE */
    const char *pattern = "*/*";
    const char *text = "a/b";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1230.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1231(void)
{
    /* Pattern: '* /*', Text: 'a/b', Flags: PATHNAME */
    const char *pattern = "*/*";
    const char *text = "a/b";
    unsigned flags = RBC_FNM_PATHNAME;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1231.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1232(void)
{
    /* Pattern: '* /*', Text: 'a/b', Flags: NOESCAPE */
    const char *pattern = "*/*";
    const char *text = "a/b";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1232.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1233(void)
{
    /* Pattern: '* /*', Text: 'a/b', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "*/*";
    const char *text = "a/b";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1233.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1234(void)
{
    /* Pattern: '* /*', Text: 'a/b', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "*/*";
    const char *text = "a/b";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1234.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1235(void)
{
    /* Pattern: '* /*', Text: 'a/b/c', Flags: NONE */
    const char *pattern = "*/*";
    const char *text = "a/b/c";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1235.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1236(void)
{
    /* Pattern: '* /*', Text: 'a/b/c', Flags: PATHNAME */
    const char *pattern = "*/*";
    const char *text = "a/b/c";
    unsigned flags = RBC_FNM_PATHNAME;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1236.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1237(void)
{
    /* Pattern: '* /*', Text: 'a/b/c', Flags: NOESCAPE */
    const char *pattern = "*/*";
    const char *text = "a/b/c";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1237.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1238(void)
{
    /* Pattern: '* /*', Text: 'a/b/c', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "*/*";
    const char *text = "a/b/c";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1238.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1239(void)
{
    /* Pattern: '* /*', Text: 'a/b/c', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "*/*";
    const char *text = "a/b/c";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1239.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1240(void)
{
    /* Pattern: '* /*.c', Text: 'src/test.c', Flags: NONE */
    const char *pattern = "*/*.c";
    const char *text = "src/test.c";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1240.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1241(void)
{
    /* Pattern: '* /*.c', Text: 'src/test.c', Flags: PATHNAME */
    const char *pattern = "*/*.c";
    const char *text = "src/test.c";
    unsigned flags = RBC_FNM_PATHNAME;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1241.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1242(void)
{
    /* Pattern: '* /*.c', Text: 'src/test.c', Flags: NOESCAPE */
    const char *pattern = "*/*.c";
    const char *text = "src/test.c";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1242.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1243(void)
{
    /* Pattern: '* /*.c', Text: 'src/test.c', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "*/*.c";
    const char *text = "src/test.c";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1243.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1244(void)
{
    /* Pattern: '* /*.c', Text: 'src/test.c', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "*/*.c";
    const char *text = "src/test.c";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1244.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1245(void)
{
    /* Pattern: '* /*.c', Text: 'test.c', Flags: NONE */
    const char *pattern = "*/*.c";
    const char *text = "test.c";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1245.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1246(void)
{
    /* Pattern: '* /*.c', Text: 'test.c', Flags: PATHNAME */
    const char *pattern = "*/*.c";
    const char *text = "test.c";
    unsigned flags = RBC_FNM_PATHNAME;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1246.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1247(void)
{
    /* Pattern: '* /*.c', Text: 'test.c', Flags: NOESCAPE */
    const char *pattern = "*/*.c";
    const char *text = "test.c";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1247.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1248(void)
{
    /* Pattern: '* /*.c', Text: 'test.c', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "*/*.c";
    const char *text = "test.c";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1248.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1249(void)
{
    /* Pattern: '* /*.c', Text: 'test.c', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "*/*.c";
    const char *text = "test.c";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1249.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1250(void)
{
    /* Pattern: 'dir/*', Text: 'dir/file', Flags: NONE */
    const char *pattern = "dir/*";
    const char *text = "dir/file";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1250.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1251(void)
{
    /* Pattern: 'dir/*', Text: 'dir/file', Flags: PATHNAME */
    const char *pattern = "dir/*";
    const char *text = "dir/file";
    unsigned flags = RBC_FNM_PATHNAME;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1251.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1252(void)
{
    /* Pattern: 'dir/*', Text: 'dir/file', Flags: NOESCAPE */
    const char *pattern = "dir/*";
    const char *text = "dir/file";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1252.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1253(void)
{
    /* Pattern: 'dir/*', Text: 'dir/file', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "dir/*";
    const char *text = "dir/file";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1253.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1254(void)
{
    /* Pattern: 'dir/*', Text: 'dir/file', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "dir/*";
    const char *text = "dir/file";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1254.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1255(void)
{
    /* Pattern: 'dir/*', Text: 'dir/sub/file', Flags: NONE */
    const char *pattern = "dir/*";
    const char *text = "dir/sub/file";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1255.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1256(void)
{
    /* Pattern: 'dir/*', Text: 'dir/sub/file', Flags: PATHNAME */
    const char *pattern = "dir/*";
    const char *text = "dir/sub/file";
    unsigned flags = RBC_FNM_PATHNAME;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1256.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1257(void)
{
    /* Pattern: 'dir/*', Text: 'dir/sub/file', Flags: NOESCAPE */
    const char *pattern = "dir/*";
    const char *text = "dir/sub/file";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1257.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1258(void)
{
    /* Pattern: 'dir/*', Text: 'dir/sub/file', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "dir/*";
    const char *text = "dir/sub/file";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1258.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1259(void)
{
    /* Pattern: 'dir/*', Text: 'dir/sub/file', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "dir/*";
    const char *text = "dir/sub/file";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1259.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1260(void)
{
    /* Pattern: '* /*.c', Text: 'src/.hidden.c', Flags: NONE */
    const char *pattern = "*/*.c";
    const char *text = "src/.hidden.c";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1260.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1261(void)
{
    /* Pattern: '* /*.c', Text: 'src/.hidden.c', Flags: PATHNAME */
    const char *pattern = "*/*.c";
    const char *text = "src/.hidden.c";
    unsigned flags = RBC_FNM_PATHNAME;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1261.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1262(void)
{
    /* Pattern: '* /*.c', Text: 'src/.hidden.c', Flags: DOTMATCH */
    const char *pattern = "*/*.c";
    const char *text = "src/.hidden.c";
    unsigned flags = RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1262.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1263(void)
{
    /* Pattern: '* /*.c', Text: 'src/.hidden.c', Flags: NOESCAPE */
    const char *pattern = "*/*.c";
    const char *text = "src/.hidden.c";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1263.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1264(void)
{
    /* Pattern: '* /*.c', Text: 'src/.hidden.c', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "*/*.c";
    const char *text = "src/.hidden.c";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1264.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1265(void)
{
    /* Pattern: '* /*.c', Text: 'src/.hidden.c', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "*/*.c";
    const char *text = "src/.hidden.c";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1265.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1266(void)
{
    /* Pattern: '.* /*.c', Text: '.hidden/test.c', Flags: NONE */
    const char *pattern = ".*/*.c";
    const char *text = ".hidden/test.c";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1266.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1267(void)
{
    /* Pattern: '.* /*.c', Text: '.hidden/test.c', Flags: PATHNAME */
    const char *pattern = ".*/*.c";
    const char *text = ".hidden/test.c";
    unsigned flags = RBC_FNM_PATHNAME;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1267.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1268(void)
{
    /* Pattern: '.* /*.c', Text: '.hidden/test.c', Flags: DOTMATCH */
    const char *pattern = ".*/*.c";
    const char *text = ".hidden/test.c";
    unsigned flags = RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1268.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1269(void)
{
    /* Pattern: '.* /*.c', Text: '.hidden/test.c', Flags: NOESCAPE */
    const char *pattern = ".*/*.c";
    const char *text = ".hidden/test.c";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1269.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1270(void)
{
    /* Pattern: '.* /*.c', Text: '.hidden/test.c', Flags: PATHNAME|DOTMATCH */
    const char *pattern = ".*/*.c";
    const char *text = ".hidden/test.c";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1270.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1271(void)
{
    /* Pattern: '.* /*.c', Text: '.hidden/test.c', Flags: PATHNAME|CASEFOLD */
    const char *pattern = ".*/*.c";
    const char *text = ".hidden/test.c";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1271.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1272(void)
{
    /* Pattern: '* /.*', Text: 'dir/.hidden', Flags: NONE */
    const char *pattern = "*/.*";
    const char *text = "dir/.hidden";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1272.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1273(void)
{
    /* Pattern: '* /.*', Text: 'dir/.hidden', Flags: PATHNAME */
    const char *pattern = "*/.*";
    const char *text = "dir/.hidden";
    unsigned flags = RBC_FNM_PATHNAME;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1273.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1274(void)
{
    /* Pattern: '* /.*', Text: 'dir/.hidden', Flags: DOTMATCH */
    const char *pattern = "*/.*";
    const char *text = "dir/.hidden";
    unsigned flags = RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1274.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1275(void)
{
    /* Pattern: '* /.*', Text: 'dir/.hidden', Flags: NOESCAPE */
    const char *pattern = "*/.*";
    const char *text = "dir/.hidden";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1275.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1276(void)
{
    /* Pattern: '* /.*', Text: 'dir/.hidden', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "*/.*";
    const char *text = "dir/.hidden";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1276.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1277(void)
{
    /* Pattern: '* /.*', Text: 'dir/.hidden', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "*/.*";
    const char *text = "dir/.hidden";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1277.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1278(void)
{
    /* Pattern: '.*', Text: '.hidden', Flags: NONE */
    const char *pattern = ".*";
    const char *text = ".hidden";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1278.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1279(void)
{
    /* Pattern: '.*', Text: '.hidden', Flags: DOTMATCH */
    const char *pattern = ".*";
    const char *text = ".hidden";
    unsigned flags = RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1279.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1280(void)
{
    /* Pattern: '.*', Text: '.hidden', Flags: NOESCAPE */
    const char *pattern = ".*";
    const char *text = ".hidden";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1280.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1281(void)
{
    /* Pattern: '.*', Text: '.hidden', Flags: PATHNAME|DOTMATCH */
    const char *pattern = ".*";
    const char *text = ".hidden";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1281.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1282(void)
{
    /* Pattern: '.*', Text: '.hidden', Flags: PATHNAME|CASEFOLD */
    const char *pattern = ".*";
    const char *text = ".hidden";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1282.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1283(void)
{
    /* Pattern: '.*', Text: 'visible', Flags: NONE */
    const char *pattern = ".*";
    const char *text = "visible";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1283.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1284(void)
{
    /* Pattern: '.*', Text: 'visible', Flags: NOESCAPE */
    const char *pattern = ".*";
    const char *text = "visible";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1284.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1285(void)
{
    /* Pattern: '.*', Text: 'visible', Flags: PATHNAME|DOTMATCH */
    const char *pattern = ".*";
    const char *text = "visible";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1285.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1286(void)
{
    /* Pattern: '.*', Text: 'visible', Flags: PATHNAME|CASEFOLD */
    const char *pattern = ".*";
    const char *text = "visible";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1286.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1287(void)
{
    /* Pattern: '.?', Text: '.a', Flags: NONE */
    const char *pattern = ".?";
    const char *text = ".a";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1287.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1288(void)
{
    /* Pattern: '.?', Text: '.a', Flags: DOTMATCH */
    const char *pattern = ".?";
    const char *text = ".a";
    unsigned flags = RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1288.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1289(void)
{
    /* Pattern: '.?', Text: '.a', Flags: NOESCAPE */
    const char *pattern = ".?";
    const char *text = ".a";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1289.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1290(void)
{
    /* Pattern: '.?', Text: '.a', Flags: PATHNAME|DOTMATCH */
    const char *pattern = ".?";
    const char *text = ".a";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1290.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1291(void)
{
    /* Pattern: '.?', Text: '.a', Flags: PATHNAME|CASEFOLD */
    const char *pattern = ".?";
    const char *text = ".a";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1291.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1292(void)
{
    /* Pattern: '.??', Text: '.ab', Flags: NONE */
    const char *pattern = ".??";
    const char *text = ".ab";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1292.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1293(void)
{
    /* Pattern: '.??', Text: '.ab', Flags: DOTMATCH */
    const char *pattern = ".??";
    const char *text = ".ab";
    unsigned flags = RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1293.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1294(void)
{
    /* Pattern: '.??', Text: '.ab', Flags: NOESCAPE */
    const char *pattern = ".??";
    const char *text = ".ab";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1294.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1295(void)
{
    /* Pattern: '.??', Text: '.ab', Flags: PATHNAME|DOTMATCH */
    const char *pattern = ".??";
    const char *text = ".ab";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1295.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1296(void)
{
    /* Pattern: '.??', Text: '.ab', Flags: PATHNAME|CASEFOLD */
    const char *pattern = ".??";
    const char *text = ".ab";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1296.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1297(void)
{
    /* Pattern: '*', Text: '.hidden', Flags: NONE */
    const char *pattern = "*";
    const char *text = ".hidden";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1297.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1298(void)
{
    /* Pattern: '*', Text: '.hidden', Flags: DOTMATCH */
    const char *pattern = "*";
    const char *text = ".hidden";
    unsigned flags = RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1298.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1299(void)
{
    /* Pattern: '*', Text: '.hidden', Flags: NOESCAPE */
    const char *pattern = "*";
    const char *text = ".hidden";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1299.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1300(void)
{
    /* Pattern: '*', Text: '.hidden', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "*";
    const char *text = ".hidden";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1300.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1301(void)
{
    /* Pattern: '*', Text: '.hidden', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "*";
    const char *text = ".hidden";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1301.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1302(void)
{
    /* Pattern: '.*', Text: '.hidden', Flags: NONE */
    const char *pattern = ".*";
    const char *text = ".hidden";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1302.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1303(void)
{
    /* Pattern: '.*', Text: '.hidden', Flags: DOTMATCH */
    const char *pattern = ".*";
    const char *text = ".hidden";
    unsigned flags = RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1303.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1304(void)
{
    /* Pattern: '.*', Text: '.hidden', Flags: NOESCAPE */
    const char *pattern = ".*";
    const char *text = ".hidden";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1304.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1305(void)
{
    /* Pattern: '.*', Text: '.hidden', Flags: PATHNAME|DOTMATCH */
    const char *pattern = ".*";
    const char *text = ".hidden";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1305.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1306(void)
{
    /* Pattern: '.*', Text: '.hidden', Flags: PATHNAME|CASEFOLD */
    const char *pattern = ".*";
    const char *text = ".hidden";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1306.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1307(void)
{
    /* Pattern: 'a/.*', Text: 'a/.hidden', Flags: NONE */
    const char *pattern = "a/.*";
    const char *text = "a/.hidden";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1307.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1308(void)
{
    /* Pattern: 'a/.*', Text: 'a/.hidden', Flags: PATHNAME */
    const char *pattern = "a/.*";
    const char *text = "a/.hidden";
    unsigned flags = RBC_FNM_PATHNAME;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1308.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1309(void)
{
    /* Pattern: 'a/.*', Text: 'a/.hidden', Flags: DOTMATCH */
    const char *pattern = "a/.*";
    const char *text = "a/.hidden";
    unsigned flags = RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1309.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1310(void)
{
    /* Pattern: 'a/.*', Text: 'a/.hidden', Flags: NOESCAPE */
    const char *pattern = "a/.*";
    const char *text = "a/.hidden";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1310.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1311(void)
{
    /* Pattern: 'a/.*', Text: 'a/.hidden', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "a/.*";
    const char *text = "a/.hidden";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1311.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1312(void)
{
    /* Pattern: 'a/.*', Text: 'a/.hidden', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "a/.*";
    const char *text = "a/.hidden";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1312.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1313(void)
{
    /* Pattern: 'test', Text: 'TEST', Flags: NONE */
    const char *pattern = "test";
    const char *text = "TEST";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1313.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1314(void)
{
    /* Pattern: 'test', Text: 'TEST', Flags: CASEFOLD */
    const char *pattern = "test";
    const char *text = "TEST";
    unsigned flags = RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1314.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1315(void)
{
    /* Pattern: 'test', Text: 'TEST', Flags: NOESCAPE */
    const char *pattern = "test";
    const char *text = "TEST";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1315.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1316(void)
{
    /* Pattern: 'test', Text: 'TEST', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "test";
    const char *text = "TEST";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1316.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1317(void)
{
    /* Pattern: 'test', Text: 'TEST', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "test";
    const char *text = "TEST";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1317.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1318(void)
{
    /* Pattern: 'test', Text: 'Test', Flags: NONE */
    const char *pattern = "test";
    const char *text = "Test";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1318.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1319(void)
{
    /* Pattern: 'test', Text: 'Test', Flags: CASEFOLD */
    const char *pattern = "test";
    const char *text = "Test";
    unsigned flags = RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1319.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1320(void)
{
    /* Pattern: 'test', Text: 'Test', Flags: NOESCAPE */
    const char *pattern = "test";
    const char *text = "Test";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1320.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1321(void)
{
    /* Pattern: 'test', Text: 'Test', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "test";
    const char *text = "Test";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1321.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1322(void)
{
    /* Pattern: 'test', Text: 'Test', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "test";
    const char *text = "Test";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1322.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1323(void)
{
    /* Pattern: 'TEST', Text: 'test', Flags: NONE */
    const char *pattern = "TEST";
    const char *text = "test";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1323.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1324(void)
{
    /* Pattern: 'TEST', Text: 'test', Flags: CASEFOLD */
    const char *pattern = "TEST";
    const char *text = "test";
    unsigned flags = RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1324.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1325(void)
{
    /* Pattern: 'TEST', Text: 'test', Flags: NOESCAPE */
    const char *pattern = "TEST";
    const char *text = "test";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1325.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1326(void)
{
    /* Pattern: 'TEST', Text: 'test', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "TEST";
    const char *text = "test";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1326.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1327(void)
{
    /* Pattern: 'TEST', Text: 'test', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "TEST";
    const char *text = "test";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1327.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1328(void)
{
    /* Pattern: '*.TXT', Text: 'file.txt', Flags: NONE */
    const char *pattern = "*.TXT";
    const char *text = "file.txt";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1328.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1329(void)
{
    /* Pattern: '*.TXT', Text: 'file.txt', Flags: CASEFOLD */
    const char *pattern = "*.TXT";
    const char *text = "file.txt";
    unsigned flags = RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1329.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1330(void)
{
    /* Pattern: '*.TXT', Text: 'file.txt', Flags: NOESCAPE */
    const char *pattern = "*.TXT";
    const char *text = "file.txt";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1330.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1331(void)
{
    /* Pattern: '*.TXT', Text: 'file.txt', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "*.TXT";
    const char *text = "file.txt";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1331.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1332(void)
{
    /* Pattern: '*.TXT', Text: 'file.txt', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "*.TXT";
    const char *text = "file.txt";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1332.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1333(void)
{
    /* Pattern: '*.txt', Text: 'FILE.TXT', Flags: NONE */
    const char *pattern = "*.txt";
    const char *text = "FILE.TXT";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1333.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1334(void)
{
    /* Pattern: '*.txt', Text: 'FILE.TXT', Flags: CASEFOLD */
    const char *pattern = "*.txt";
    const char *text = "FILE.TXT";
    unsigned flags = RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1334.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1335(void)
{
    /* Pattern: '*.txt', Text: 'FILE.TXT', Flags: NOESCAPE */
    const char *pattern = "*.txt";
    const char *text = "FILE.TXT";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1335.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1336(void)
{
    /* Pattern: '*.txt', Text: 'FILE.TXT', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "*.txt";
    const char *text = "FILE.TXT";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1336.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1337(void)
{
    /* Pattern: '*.txt', Text: 'FILE.TXT', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "*.txt";
    const char *text = "FILE.TXT";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1337.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1338(void)
{
    /* Pattern: '', Text: '', Flags: NONE */
    const char *pattern = "";
    const char *text = "";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1338.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1339(void)
{
    /* Pattern: '', Text: '', Flags: NOESCAPE */
    const char *pattern = "";
    const char *text = "";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1339.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1340(void)
{
    /* Pattern: '', Text: '', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "";
    const char *text = "";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1340.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1341(void)
{
    /* Pattern: '', Text: '', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "";
    const char *text = "";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1341.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1342(void)
{
    /* Pattern: '', Text: 'x', Flags: NONE */
    const char *pattern = "";
    const char *text = "x";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1342.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1343(void)
{
    /* Pattern: '', Text: 'x', Flags: NOESCAPE */
    const char *pattern = "";
    const char *text = "x";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1343.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1344(void)
{
    /* Pattern: '', Text: 'x', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "";
    const char *text = "x";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1344.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1345(void)
{
    /* Pattern: '', Text: 'x', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "";
    const char *text = "x";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1345.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1346(void)
{
    /* Pattern: 'x', Text: '', Flags: NONE */
    const char *pattern = "x";
    const char *text = "";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1346.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1347(void)
{
    /* Pattern: 'x', Text: '', Flags: NOESCAPE */
    const char *pattern = "x";
    const char *text = "";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1347.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1348(void)
{
    /* Pattern: 'x', Text: '', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "x";
    const char *text = "";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1348.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1349(void)
{
    /* Pattern: 'x', Text: '', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "x";
    const char *text = "";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1349.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1350(void)
{
    /* Pattern: 'aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa', Text: 'aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa', Flags: NONE */
    const char *pattern = "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa";
    const char *text = "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1350.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1351(void)
{
    /* Pattern: 'aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa', Text: 'aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa', Flags: NOESCAPE */
    const char *pattern = "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa";
    const char *text = "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1351.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1352(void)
{
    /* Pattern: 'aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa', Text: 'aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa";
    const char *text = "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1352.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1353(void)
{
    /* Pattern: 'aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa', Text: 'aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa";
    const char *text = "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1353.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1354(void)
{
    /* Pattern: 'aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa', Text: 'aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa', Flags: NONE */
    const char *pattern = "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa";
    const char *text = "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1354.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1355(void)
{
    /* Pattern: 'aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa', Text: 'aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa', Flags: NOESCAPE */
    const char *pattern = "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa";
    const char *text = "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1355.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1356(void)
{
    /* Pattern: 'aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa', Text: 'aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa";
    const char *text = "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1356.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1357(void)
{
    /* Pattern: 'aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa', Text: 'aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa";
    const char *text = "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1357.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1358(void)
{
    /* Pattern: '*aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa', Text: 'xaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa', Flags: NONE */
    const char *pattern = "*aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa";
    const char *text = "xaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1358.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1359(void)
{
    /* Pattern: '*aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa', Text: 'xaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa', Flags: NOESCAPE */
    const char *pattern = "*aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa";
    const char *text = "xaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1359.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1360(void)
{
    /* Pattern: '*aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa', Text: 'xaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "*aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa";
    const char *text = "xaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1360.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1361(void)
{
    /* Pattern: '*aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa', Text: 'xaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "*aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa";
    const char *text = "xaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1361.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1362(void)
{
    /* Pattern: 'file with spaces.txt', Text: 'file with spaces.txt', Flags: NONE */
    const char *pattern = "file with spaces.txt";
    const char *text = "file with spaces.txt";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1362.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1363(void)
{
    /* Pattern: 'file with spaces.txt', Text: 'file with spaces.txt', Flags: NOESCAPE */
    const char *pattern = "file with spaces.txt";
    const char *text = "file with spaces.txt";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1363.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1364(void)
{
    /* Pattern: 'file with spaces.txt', Text: 'file with spaces.txt', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "file with spaces.txt";
    const char *text = "file with spaces.txt";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1364.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1365(void)
{
    /* Pattern: 'file with spaces.txt', Text: 'file with spaces.txt', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "file with spaces.txt";
    const char *text = "file with spaces.txt";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1365.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1366(void)
{
    /* Pattern: 'file	with	tabs', Text: 'file	with	tabs', Flags: NONE */
    const char *pattern = "file\twith\ttabs";
    const char *text = "file\twith\ttabs";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1366.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1367(void)
{
    /* Pattern: 'file	with	tabs', Text: 'file	with	tabs', Flags: NOESCAPE */
    const char *pattern = "file\twith\ttabs";
    const char *text = "file\twith\ttabs";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1367.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1368(void)
{
    /* Pattern: 'file	with	tabs', Text: 'file	with	tabs', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "file\twith\ttabs";
    const char *text = "file\twith\ttabs";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1368.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1369(void)
{
    /* Pattern: 'file	with	tabs', Text: 'file	with	tabs', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "file\twith\ttabs";
    const char *text = "file\twith\ttabs";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1369.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1370(void)
{
    /* Pattern: 'multiple..dots', Text: 'multiple..dots', Flags: NONE */
    const char *pattern = "multiple..dots";
    const char *text = "multiple..dots";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1370.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1371(void)
{
    /* Pattern: 'multiple..dots', Text: 'multiple..dots', Flags: NOESCAPE */
    const char *pattern = "multiple..dots";
    const char *text = "multiple..dots";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1371.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1372(void)
{
    /* Pattern: 'multiple..dots', Text: 'multiple..dots', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "multiple..dots";
    const char *text = "multiple..dots";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1372.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1373(void)
{
    /* Pattern: 'multiple..dots', Text: 'multiple..dots', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "multiple..dots";
    const char *text = "multiple..dots";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1373.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1374(void)
{
    /* Pattern: '*.txt', Text: '日本語.txt', Flags: NONE */
    const char *pattern = "*.txt";
    const char *text = "日本語.txt";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1374.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1375(void)
{
    /* Pattern: '*.txt', Text: '日本語.txt', Flags: NOESCAPE */
    const char *pattern = "*.txt";
    const char *text = "日本語.txt";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1375.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1376(void)
{
    /* Pattern: '*.txt', Text: '日本語.txt', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "*.txt";
    const char *text = "日本語.txt";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1376.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1377(void)
{
    /* Pattern: '*.txt', Text: '日本語.txt', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "*.txt";
    const char *text = "日本語.txt";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1377.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1378(void)
{
    /* Pattern: '日本語*', Text: '日本語ファイル', Flags: NONE */
    const char *pattern = "日本語*";
    const char *text = "日本語ファイル";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1378.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1379(void)
{
    /* Pattern: '日本語*', Text: '日本語ファイル', Flags: NOESCAPE */
    const char *pattern = "日本語*";
    const char *text = "日本語ファイル";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1379.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1380(void)
{
    /* Pattern: '日本語*', Text: '日本語ファイル', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "日本語*";
    const char *text = "日本語ファイル";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1380.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1381(void)
{
    /* Pattern: '日本語*', Text: '日本語ファイル', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "日本語*";
    const char *text = "日本語ファイル";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1381.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1382(void)
{
    /* Pattern: '日本語*', Text: '日本語', Flags: NONE */
    const char *pattern = "日本語*";
    const char *text = "日本語";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1382.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1383(void)
{
    /* Pattern: '日本語*', Text: '日本語', Flags: NOESCAPE */
    const char *pattern = "日本語*";
    const char *text = "日本語";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1383.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1384(void)
{
    /* Pattern: '日本語*', Text: '日本語', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "日本語*";
    const char *text = "日本語";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1384.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1385(void)
{
    /* Pattern: '日本語*', Text: '日本語', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "日本語*";
    const char *text = "日本語";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1385.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1386(void)
{
    /* Pattern: '** /*.c', Text: 'a/b/test.c', Flags: NONE */
    const char *pattern = "**/*.c";
    const char *text = "a/b/test.c";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1386.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1387(void)
{
    /* Pattern: '** /*.c', Text: 'a/b/test.c', Flags: PATHNAME */
    const char *pattern = "**/*.c";
    const char *text = "a/b/test.c";
    unsigned flags = RBC_FNM_PATHNAME;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1387.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1388(void)
{
    /* Pattern: '** /*.c', Text: 'a/b/test.c', Flags: NOESCAPE */
    const char *pattern = "**/*.c";
    const char *text = "a/b/test.c";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1388.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1389(void)
{
    /* Pattern: '** /*.c', Text: 'a/b/test.c', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "**/*.c";
    const char *text = "a/b/test.c";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1389.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1390(void)
{
    /* Pattern: '** /*.c', Text: 'a/b/test.c', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "**/*.c";
    const char *text = "a/b/test.c";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1390.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1391(void)
{
    /* Pattern: 'a*b*c', Text: 'abc', Flags: NONE */
    const char *pattern = "a*b*c";
    const char *text = "abc";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1391.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1392(void)
{
    /* Pattern: 'a*b*c', Text: 'abc', Flags: NOESCAPE */
    const char *pattern = "a*b*c";
    const char *text = "abc";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1392.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1393(void)
{
    /* Pattern: 'a*b*c', Text: 'abc', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "a*b*c";
    const char *text = "abc";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1393.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1394(void)
{
    /* Pattern: 'a*b*c', Text: 'abc', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "a*b*c";
    const char *text = "abc";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1394.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1395(void)
{
    /* Pattern: 'a*b*c', Text: 'aXbYc', Flags: NONE */
    const char *pattern = "a*b*c";
    const char *text = "aXbYc";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1395.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1396(void)
{
    /* Pattern: 'a*b*c', Text: 'aXbYc', Flags: CASEFOLD */
    const char *pattern = "a*b*c";
    const char *text = "aXbYc";
    unsigned flags = RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1396.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1397(void)
{
    /* Pattern: 'a*b*c', Text: 'aXbYc', Flags: NOESCAPE */
    const char *pattern = "a*b*c";
    const char *text = "aXbYc";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1397.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1398(void)
{
    /* Pattern: 'a*b*c', Text: 'aXbYc', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "a*b*c";
    const char *text = "aXbYc";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1398.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1399(void)
{
    /* Pattern: 'a*b*c', Text: 'aXbYc', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "a*b*c";
    const char *text = "aXbYc";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1399.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1400(void)
{
    /* Pattern: '*.*.*', Text: 'a.b.c', Flags: NONE */
    const char *pattern = "*.*.*";
    const char *text = "a.b.c";
    unsigned flags = 0;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1400.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1401(void)
{
    /* Pattern: '*.*.*', Text: 'a.b.c', Flags: NOESCAPE */
    const char *pattern = "*.*.*";
    const char *text = "a.b.c";
    unsigned flags = RBC_FNM_NOESCAPE;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1401.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1402(void)
{
    /* Pattern: '*.*.*', Text: 'a.b.c', Flags: PATHNAME|DOTMATCH */
    const char *pattern = "*.*.*";
    const char *text = "a.b.c";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_DOTMATCH;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1402.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


void test_f1403(void)
{
    /* Pattern: '*.*.*', Text: 'a.b.c', Flags: PATHNAME|CASEFOLD */
    const char *pattern = "*.*.*";
    const char *text = "a.b.c";
    unsigned flags = RBC_FNM_PATHNAME | RBC_FNM_CASEFOLD;
    const char *expected_file = "tests/ruby_fnmatch_expected/f1403.txt";

    /* Load Ruby expected result */
    FILE *fp = fopen(expected_file, "r");
    TEST_ASSERT_NOT_NULL_MESSAGE(fp, "Failed to open expected file");

    char expected_str[16];
    TEST_ASSERT_NOT_NULL(fgets(expected_str, sizeof(expected_str), fp));
    fclose(fp);

    /* Remove trailing newline */
    size_t len = strlen(expected_str);
    if (len > 0 && expected_str[len - 1] == '\n') {
        expected_str[len - 1] = '\0';
    }

    bool expected = (strcmp(expected_str, "true") == 0);

    /* Execute rbc_fnmatch */
    bool result = rbc_fnmatch(pattern, text, flags);

    /* Compare with Ruby result */
    TEST_ASSERT_EQUAL_MESSAGE(expected, result, "Result mismatch with Ruby File.fnmatch");
}


/* ========================================================================
 * Unity Runner
 * ======================================================================== */

int main(void)
{
    UNITY_BEGIN();

    RUN_TEST(test_fm001);
    RUN_TEST(test_fm002);
    RUN_TEST(test_fm003);
    RUN_TEST(test_fm010);
    RUN_TEST(test_fm011);
    RUN_TEST(test_fm012);
    RUN_TEST(test_fm020);
    RUN_TEST(test_fm021);
    RUN_TEST(test_fm022);
    RUN_TEST(test_fm030);
    RUN_TEST(test_fm031);
    RUN_TEST(test_fm032);
    RUN_TEST(test_fm040);
    RUN_TEST(test_fm041);
    RUN_TEST(test_f1000);
    RUN_TEST(test_f1001);
    RUN_TEST(test_f1002);
    RUN_TEST(test_f1003);
    RUN_TEST(test_f1004);
    RUN_TEST(test_f1005);
    RUN_TEST(test_f1006);
    RUN_TEST(test_f1007);
    RUN_TEST(test_f1008);
    RUN_TEST(test_f1009);
    RUN_TEST(test_f1010);
    RUN_TEST(test_f1011);
    RUN_TEST(test_f1012);
    RUN_TEST(test_f1013);
    RUN_TEST(test_f1014);
    RUN_TEST(test_f1015);
    RUN_TEST(test_f1016);
    RUN_TEST(test_f1017);
    RUN_TEST(test_f1018);
    RUN_TEST(test_f1019);
    RUN_TEST(test_f1020);
    RUN_TEST(test_f1021);
    RUN_TEST(test_f1022);
    RUN_TEST(test_f1023);
    RUN_TEST(test_f1024);
    RUN_TEST(test_f1025);
    RUN_TEST(test_f1026);
    RUN_TEST(test_f1027);
    RUN_TEST(test_f1028);
    RUN_TEST(test_f1029);
    RUN_TEST(test_f1030);
    RUN_TEST(test_f1031);
    RUN_TEST(test_f1032);
    RUN_TEST(test_f1033);
    RUN_TEST(test_f1034);
    RUN_TEST(test_f1035);
    RUN_TEST(test_f1036);
    RUN_TEST(test_f1037);
    RUN_TEST(test_f1038);
    RUN_TEST(test_f1039);
    RUN_TEST(test_f1040);
    RUN_TEST(test_f1041);
    RUN_TEST(test_f1042);
    RUN_TEST(test_f1043);
    RUN_TEST(test_f1044);
    RUN_TEST(test_f1045);
    RUN_TEST(test_f1046);
    RUN_TEST(test_f1047);
    RUN_TEST(test_f1048);
    RUN_TEST(test_f1049);
    RUN_TEST(test_f1050);
    RUN_TEST(test_f1051);
    RUN_TEST(test_f1052);
    RUN_TEST(test_f1053);
    RUN_TEST(test_f1054);
    RUN_TEST(test_f1055);
    RUN_TEST(test_f1056);
    RUN_TEST(test_f1057);
    RUN_TEST(test_f1058);
    RUN_TEST(test_f1059);
    RUN_TEST(test_f1060);
    RUN_TEST(test_f1061);
    RUN_TEST(test_f1062);
    RUN_TEST(test_f1063);
    RUN_TEST(test_f1064);
    RUN_TEST(test_f1065);
    RUN_TEST(test_f1066);
    RUN_TEST(test_f1067);
    RUN_TEST(test_f1068);
    RUN_TEST(test_f1069);
    RUN_TEST(test_f1070);
    RUN_TEST(test_f1071);
    RUN_TEST(test_f1072);
    RUN_TEST(test_f1073);
    RUN_TEST(test_f1074);
    RUN_TEST(test_f1075);
    RUN_TEST(test_f1076);
    RUN_TEST(test_f1077);
    RUN_TEST(test_f1078);
    RUN_TEST(test_f1079);
    RUN_TEST(test_f1080);
    RUN_TEST(test_f1081);
    RUN_TEST(test_f1082);
    RUN_TEST(test_f1083);
    RUN_TEST(test_f1084);
    RUN_TEST(test_f1085);
    RUN_TEST(test_f1086);
    RUN_TEST(test_f1087);
    RUN_TEST(test_f1088);
    RUN_TEST(test_f1089);
    RUN_TEST(test_f1090);
    RUN_TEST(test_f1091);
    RUN_TEST(test_f1092);
    RUN_TEST(test_f1093);
    RUN_TEST(test_f1094);
    RUN_TEST(test_f1095);
    RUN_TEST(test_f1096);
    RUN_TEST(test_f1097);
    RUN_TEST(test_f1098);
    RUN_TEST(test_f1099);
    RUN_TEST(test_f1100);
    RUN_TEST(test_f1101);
    RUN_TEST(test_f1102);
    RUN_TEST(test_f1103);
    RUN_TEST(test_f1104);
    RUN_TEST(test_f1105);
    RUN_TEST(test_f1106);
    RUN_TEST(test_f1107);
    RUN_TEST(test_f1108);
    RUN_TEST(test_f1109);
    RUN_TEST(test_f1110);
    RUN_TEST(test_f1111);
    RUN_TEST(test_f1112);
    RUN_TEST(test_f1113);
    RUN_TEST(test_f1114);
    RUN_TEST(test_f1115);
    RUN_TEST(test_f1116);
    RUN_TEST(test_f1117);
    RUN_TEST(test_f1118);
    RUN_TEST(test_f1119);
    RUN_TEST(test_f1120);
    RUN_TEST(test_f1121);
    RUN_TEST(test_f1122);
    RUN_TEST(test_f1123);
    RUN_TEST(test_f1124);
    RUN_TEST(test_f1125);
    RUN_TEST(test_f1126);
    RUN_TEST(test_f1127);
    RUN_TEST(test_f1128);
    RUN_TEST(test_f1129);
    RUN_TEST(test_f1130);
    RUN_TEST(test_f1131);
    RUN_TEST(test_f1132);
    RUN_TEST(test_f1133);
    RUN_TEST(test_f1134);
    RUN_TEST(test_f1135);
    RUN_TEST(test_f1136);
    RUN_TEST(test_f1137);
    RUN_TEST(test_f1138);
    RUN_TEST(test_f1139);
    RUN_TEST(test_f1140);
    RUN_TEST(test_f1141);
    RUN_TEST(test_f1142);
    RUN_TEST(test_f1143);
    RUN_TEST(test_f1144);
    RUN_TEST(test_f1145);
    RUN_TEST(test_f1146);
    RUN_TEST(test_f1147);
    RUN_TEST(test_f1148);
    RUN_TEST(test_f1149);
    RUN_TEST(test_f1150);
    RUN_TEST(test_f1151);
    RUN_TEST(test_f1152);
    RUN_TEST(test_f1153);
    RUN_TEST(test_f1154);
    RUN_TEST(test_f1155);
    RUN_TEST(test_f1156);
    RUN_TEST(test_f1157);
    RUN_TEST(test_f1158);
    RUN_TEST(test_f1159);
    RUN_TEST(test_f1160);
    RUN_TEST(test_f1161);
    RUN_TEST(test_f1162);
    RUN_TEST(test_f1163);
    RUN_TEST(test_f1164);
    RUN_TEST(test_f1165);
    RUN_TEST(test_f1166);
    RUN_TEST(test_f1167);
    RUN_TEST(test_f1168);
    RUN_TEST(test_f1169);
    RUN_TEST(test_f1170);
    RUN_TEST(test_f1171);
    RUN_TEST(test_f1172);
    RUN_TEST(test_f1173);
    RUN_TEST(test_f1174);
    RUN_TEST(test_f1175);
    RUN_TEST(test_f1176);
    RUN_TEST(test_f1177);
    RUN_TEST(test_f1178);
    RUN_TEST(test_f1179);
    RUN_TEST(test_f1180);
    RUN_TEST(test_f1181);
    RUN_TEST(test_f1182);
    RUN_TEST(test_f1183);
    RUN_TEST(test_f1184);
    RUN_TEST(test_f1185);
    RUN_TEST(test_f1186);
    RUN_TEST(test_f1187);
    RUN_TEST(test_f1188);
    RUN_TEST(test_f1189);
    RUN_TEST(test_f1190);
    RUN_TEST(test_f1191);
    RUN_TEST(test_f1192);
    RUN_TEST(test_f1193);
    RUN_TEST(test_f1194);
    RUN_TEST(test_f1195);
    RUN_TEST(test_f1196);
    RUN_TEST(test_f1197);
    RUN_TEST(test_f1198);
    RUN_TEST(test_f1199);
    RUN_TEST(test_f1200);
    RUN_TEST(test_f1201);
    RUN_TEST(test_f1202);
    RUN_TEST(test_f1203);
    RUN_TEST(test_f1204);
    RUN_TEST(test_f1205);
    RUN_TEST(test_f1206);
    RUN_TEST(test_f1207);
    RUN_TEST(test_f1208);
    RUN_TEST(test_f1209);
    RUN_TEST(test_f1210);
    RUN_TEST(test_f1211);
    RUN_TEST(test_f1212);
    RUN_TEST(test_f1213);
    RUN_TEST(test_f1214);
    RUN_TEST(test_f1215);
    RUN_TEST(test_f1216);
    RUN_TEST(test_f1217);
    RUN_TEST(test_f1218);
    RUN_TEST(test_f1219);
    RUN_TEST(test_f1220);
    RUN_TEST(test_f1221);
    RUN_TEST(test_f1222);
    RUN_TEST(test_f1223);
    RUN_TEST(test_f1224);
    RUN_TEST(test_f1225);
    RUN_TEST(test_f1226);
    RUN_TEST(test_f1227);
    RUN_TEST(test_f1228);
    RUN_TEST(test_f1229);
    RUN_TEST(test_f1230);
    RUN_TEST(test_f1231);
    RUN_TEST(test_f1232);
    RUN_TEST(test_f1233);
    RUN_TEST(test_f1234);
    RUN_TEST(test_f1235);
    RUN_TEST(test_f1236);
    RUN_TEST(test_f1237);
    RUN_TEST(test_f1238);
    RUN_TEST(test_f1239);
    RUN_TEST(test_f1240);
    RUN_TEST(test_f1241);
    RUN_TEST(test_f1242);
    RUN_TEST(test_f1243);
    RUN_TEST(test_f1244);
    RUN_TEST(test_f1245);
    RUN_TEST(test_f1246);
    RUN_TEST(test_f1247);
    RUN_TEST(test_f1248);
    RUN_TEST(test_f1249);
    RUN_TEST(test_f1250);
    RUN_TEST(test_f1251);
    RUN_TEST(test_f1252);
    RUN_TEST(test_f1253);
    RUN_TEST(test_f1254);
    RUN_TEST(test_f1255);
    RUN_TEST(test_f1256);
    RUN_TEST(test_f1257);
    RUN_TEST(test_f1258);
    RUN_TEST(test_f1259);
    RUN_TEST(test_f1260);
    RUN_TEST(test_f1261);
    RUN_TEST(test_f1262);
    RUN_TEST(test_f1263);
    RUN_TEST(test_f1264);
    RUN_TEST(test_f1265);
    RUN_TEST(test_f1266);
    RUN_TEST(test_f1267);
    RUN_TEST(test_f1268);
    RUN_TEST(test_f1269);
    RUN_TEST(test_f1270);
    RUN_TEST(test_f1271);
    RUN_TEST(test_f1272);
    RUN_TEST(test_f1273);
    RUN_TEST(test_f1274);
    RUN_TEST(test_f1275);
    RUN_TEST(test_f1276);
    RUN_TEST(test_f1277);
    RUN_TEST(test_f1278);
    RUN_TEST(test_f1279);
    RUN_TEST(test_f1280);
    RUN_TEST(test_f1281);
    RUN_TEST(test_f1282);
    RUN_TEST(test_f1283);
    RUN_TEST(test_f1284);
    RUN_TEST(test_f1285);
    RUN_TEST(test_f1286);
    RUN_TEST(test_f1287);
    RUN_TEST(test_f1288);
    RUN_TEST(test_f1289);
    RUN_TEST(test_f1290);
    RUN_TEST(test_f1291);
    RUN_TEST(test_f1292);
    RUN_TEST(test_f1293);
    RUN_TEST(test_f1294);
    RUN_TEST(test_f1295);
    RUN_TEST(test_f1296);
    RUN_TEST(test_f1297);
    RUN_TEST(test_f1298);
    RUN_TEST(test_f1299);
    RUN_TEST(test_f1300);
    RUN_TEST(test_f1301);
    RUN_TEST(test_f1302);
    RUN_TEST(test_f1303);
    RUN_TEST(test_f1304);
    RUN_TEST(test_f1305);
    RUN_TEST(test_f1306);
    RUN_TEST(test_f1307);
    RUN_TEST(test_f1308);
    RUN_TEST(test_f1309);
    RUN_TEST(test_f1310);
    RUN_TEST(test_f1311);
    RUN_TEST(test_f1312);
    RUN_TEST(test_f1313);
    RUN_TEST(test_f1314);
    RUN_TEST(test_f1315);
    RUN_TEST(test_f1316);
    RUN_TEST(test_f1317);
    RUN_TEST(test_f1318);
    RUN_TEST(test_f1319);
    RUN_TEST(test_f1320);
    RUN_TEST(test_f1321);
    RUN_TEST(test_f1322);
    RUN_TEST(test_f1323);
    RUN_TEST(test_f1324);
    RUN_TEST(test_f1325);
    RUN_TEST(test_f1326);
    RUN_TEST(test_f1327);
    RUN_TEST(test_f1328);
    RUN_TEST(test_f1329);
    RUN_TEST(test_f1330);
    RUN_TEST(test_f1331);
    RUN_TEST(test_f1332);
    RUN_TEST(test_f1333);
    RUN_TEST(test_f1334);
    RUN_TEST(test_f1335);
    RUN_TEST(test_f1336);
    RUN_TEST(test_f1337);
    RUN_TEST(test_f1338);
    RUN_TEST(test_f1339);
    RUN_TEST(test_f1340);
    RUN_TEST(test_f1341);
    RUN_TEST(test_f1342);
    RUN_TEST(test_f1343);
    RUN_TEST(test_f1344);
    RUN_TEST(test_f1345);
    RUN_TEST(test_f1346);
    RUN_TEST(test_f1347);
    RUN_TEST(test_f1348);
    RUN_TEST(test_f1349);
    RUN_TEST(test_f1350);
    RUN_TEST(test_f1351);
    RUN_TEST(test_f1352);
    RUN_TEST(test_f1353);
    RUN_TEST(test_f1354);
    RUN_TEST(test_f1355);
    RUN_TEST(test_f1356);
    RUN_TEST(test_f1357);
    RUN_TEST(test_f1358);
    RUN_TEST(test_f1359);
    RUN_TEST(test_f1360);
    RUN_TEST(test_f1361);
    RUN_TEST(test_f1362);
    RUN_TEST(test_f1363);
    RUN_TEST(test_f1364);
    RUN_TEST(test_f1365);
    RUN_TEST(test_f1366);
    RUN_TEST(test_f1367);
    RUN_TEST(test_f1368);
    RUN_TEST(test_f1369);
    RUN_TEST(test_f1370);
    RUN_TEST(test_f1371);
    RUN_TEST(test_f1372);
    RUN_TEST(test_f1373);
    RUN_TEST(test_f1374);
    RUN_TEST(test_f1375);
    RUN_TEST(test_f1376);
    RUN_TEST(test_f1377);
    RUN_TEST(test_f1378);
    RUN_TEST(test_f1379);
    RUN_TEST(test_f1380);
    RUN_TEST(test_f1381);
    RUN_TEST(test_f1382);
    RUN_TEST(test_f1383);
    RUN_TEST(test_f1384);
    RUN_TEST(test_f1385);
    RUN_TEST(test_f1386);
    RUN_TEST(test_f1387);
    RUN_TEST(test_f1388);
    RUN_TEST(test_f1389);
    RUN_TEST(test_f1390);
    RUN_TEST(test_f1391);
    RUN_TEST(test_f1392);
    RUN_TEST(test_f1393);
    RUN_TEST(test_f1394);
    RUN_TEST(test_f1395);
    RUN_TEST(test_f1396);
    RUN_TEST(test_f1397);
    RUN_TEST(test_f1398);
    RUN_TEST(test_f1399);
    RUN_TEST(test_f1400);
    RUN_TEST(test_f1401);
    RUN_TEST(test_f1402);
    RUN_TEST(test_f1403);
    return UNITY_END();
}
