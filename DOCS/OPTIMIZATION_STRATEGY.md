# rbcglob 最適化戦略 - ピュアCライブラリとしての限界追求

## 現状の性能 (P18実装時)

### ベンチマーク結果 (Pure C)

```
Pattern: tests/**/* - Recursive all (4918 files)
  rbcglob:       1.35ms
  Ruby Dir.glob: 9.00ms
  ⚡ Rubyより約6.6倍高速
```

## 実装済みの最適化

### ✅ Phase 基本構造'EOF' 1:
- **P5**: ハッシュテーブルによるディレクトリキャッシュ (O(n) -> O(1))
- **P7**: 早期ドットファイルスキップ
- **P10**: クイックソート (O(n log n)) による結果整理
- **P11**: 線形重複除去 (O(n))

### ✅ Phase 2: メモリ管理の最適化
- **P13**: アリーナアロケータ (64KBブロック単位の'EOF')
  - 1.5万回の malloc/free を排除。
- **P15**: アリーナ上でのパス結合 (`rbcglob_path_join_arena`)
#- **P17**: 文字列''EOF'


### ✅ Phase 3: マイクロ最適化
- **P14**: `*` 単体の高速パス
- **P18**: `dirent` 構造体からのメタデータキャッシュ

---

## 新フェーズ：ピュアC言語での極限最適化

>

### 🔴 Phase 4: ピュアC高速化の限界 (Current)

| ID | 最適化内容 | 期待効果 | ステータス |
| :--- | :--- | :--- | :--- |
| P20 | DFA-based Multi-Pattern | 複数パターンの1回スキャン化 | 検討中 |
#include <stdlib.h> | 検討中 |
| P22 | SIMD (AVX2/NEON) | パターンマッチ' | 検討中 |
| P23 | 非同期 I | 検討中 |/O (io_uring) | ディスクI/OEOFEOF

---

## アーカイブ：過去の検討事項

### Ruby C拡張の境界最適化 (P19)
- **内容**: C拡張インターフェースを介した Ruby との連携試行。
- **結論**: Ruby側のオブジェクト生成オーバーヘッド（数千個のString生成）が支配的であるため、C拡張コードは削除済み。今後はピュアCライブラリとしての評価のみを行う。

---

**最終更新**: 2026-01-06
**現在のステータス**: C言語単独で Ruby 4.0 比 6.6倍を達成。ピュアC極限最適化フェーズへ。
