# glob互換テスト - 詳細な失敗原因分析

日付: 2026-01-22
実行環境: テストフィクスチャ `/workspaces/dirglob/tests/fixtures`

## 実装動作の検証結果

### パターン1: `07_recursive/**/` (ディレクトリ再帰)

**期待値 (Ruby):** 40件
```ruby
Dir.glob('07_recursive/**/', File::FNM_DOTMATCH).count
# => 40
```

**実際の値 (rbcglob):** 4件
```
07_recursive/./
07_recursive/dir0/
07_recursive/dir1/
07_recursive/dir2/
```

**問題:**
- `**`が再帰的にディレクトリを探索していない
- 最上位のディレクトリのみマッチしている
- サブディレクトリ (`dir0/dir0/`, `dir0/dir1/`など) が見つかっていない

### パターン2: `07_recursive/**/**` (二重再帰)

**期待値 (Ruby):** 160件
```ruby
Dir.glob('07_recursive/**/**', File::FNM_DOTMATCH).count
# => 160
```

**実際の値 (rbcglob):** 25件
```
07_recursive/.
07_recursive/dir0
07_recursive/dir0/dir0
...（第2階層まで）
```

**問題:**
- やはり深い階層まで再帰していない
- 期待値160に対して25件のみ = 84%の結果が欠落
- `**`が1階層分しか機能していない可能性

### パターン3: `.*` (ドットファイル/ディレクトリ)

**期待値 (Ruby):** 38件 (推測: テストによって変動)
**実際の値 (rbcglob):** 38件

**状況:**
- カウントは一致している場合が多い
- しかし、内容不一致が発生するケースあり
- ソート順序の問題の可能性

### パターン4: `./*` (カレントディレクトリ相対)

**期待値 (Ruby):** 0件 (相対パスは使用しない設定の可能性)
**実際の値 (rbcglob):** 61件

**問題:**
- Rubyでは`./*`が特殊な扱いを受けている可能性
- 実装は`./`を通常のパス要素として扱っている
- テストの期待値は「fixturesディレクトリからの相対パスで`./`付き」を期待していない

### パターン5: `/workspaces/dirglob/tests/fixtures/*` (絶対パス)

**期待値 (Ruby):** 21件以上
**実際の値 (rbcglob):** 0件

**問題:**
- **絶対パスが全く機能していない**
- baseパラメータとパターンの組み合わせが不適切
- `base="."` で絶対パスパターンを使用すると、`.` + 絶対パス = 誤ったパスになる

## 根本原因の特定

### 原因1: `**`の再帰処理の不完全実装

現在の実装では、`**`が次のような動作をしている：
1. 最初の階層のディレクトリを列挙
2. しかし、それ以降の再帰的な探索が行われていない
3. `**/**`のような複数`**`パターンも正しく処理されていない

**必要な修正:**
- `**`がマッチした時点で、その配下のすべてのディレクトリを再帰的に探索
- `**`は「0個以上のディレクトリ階層」にマッチする必要がある

### 原因2: 絶対パスとbaseパラメータの不整合

```c
rbc_glob(&pattern, 1, flags, ".", true, &files, &count, NULL);
```

この呼び出しで、`base="."` を指定しているため:
- パターン: `/workspaces/dirglob/tests/fixtures/*`
- base: `.`
- 実際の探索パス: `.` + `/workspaces/...` → 不正なパス

**必要な修正:**
- 絶対パスの場合は`base`を無視するか、自動的に`base=""`とする
- または、パターンが絶対パスかを判定し、適切に処理

### 原因3: `./`パス接頭辞の扱い

Rubyのテストでは、`./*`パターンは特別な意味を持っているようだが、実装は文字通りに処理している。

**必要な修正:**
- `./*`の特殊処理を追加
- または、テスト生成側でRubyの挙動に合わせた期待値を生成

### 原因4: 複数スラッシュの正規化

```
期待: 02_asterisk/dir0//subdir0///file0.txt
実際: 02_asterisk/dir0/subdir0/file0.txt
```

実装がパスを正規化しているが、Rubyは連続スラッシュを保持する。

**必要な修正:**
- パス正規化を行わないオプション
- または、結果に連続スラッシュを保持する処理

## 優先度付き修正計画

### 🔴 緊急 (影響が大きく、基本機能)

#### 1. `**`の再帰処理実装
**影響:** 714件の失敗 (全失敗の40%)
**ファイル:** `src/core/glob.c` の`**`処理部分
**修正内容:**
- `**`マッチ時に再帰的にサブディレクトリを探索
- 各サブディレクトリでパターンの残り部分を評価
- `**/**`のような複数`**`パターンのサポート

**実装例:**
```c
// **がマッチした時
if (is_doublestar) {
    // 現在のディレクトリでパターンの残りを評価
    evaluate_pattern(current_dir, remaining_pattern);

    // すべてのサブディレクトリに対して再帰
    for (each subdir in current_dir) {
        recursively_match(subdir, full_pattern_with_doublestar);
    }
}
```

#### 2. 絶対パス処理の修正
**影響:** 132件の失敗
**ファイル:** `src/core/glob.c` のパス結合処理
**修正内容:**
```c
bool is_absolute_path(const char *pattern) {
    return pattern[0] == '/';
}

// glob実行時
if (is_absolute_path(pattern)) {
    base = "";  // 絶対パスの場合はbaseを無視
}
```

### 🟡 重要 (機能の正確性)

#### 3. ドットファイル処理の精緻化
**影響:** 約600件（`**`との複合問題を除く）
**修正内容:**
- FNM_DOTMATCHフラグ時の`.*`と`.`の扱い
- `./`ディレクトリの正確な処理

#### 4. ディレクトリフィルタ（末尾`/`）
**影響:** 495件
**修正内容:**
- パターンが`/`で終わる場合、ディレクトリのみを返す
- stat()でファイルタイプを確認

### 🟢 低優先 (エッジケース、互換性)

#### 5. 複数スラッシュの保持
**影響:** 48件
**修正内容:**
- パス正規化をスキップするオプション

#### 6. ソート順序の調整
**影響:** 約100件
**修正内容:**
- Rubyと同じlocale設定でのソート

## 性能への影響予測

### `**`再帰処理の改善
- **現在:** O(n) - 1階層のみ
- **修正後:** O(n * d) - n=ファイル数, d=ディレクトリ深さ
- **対策:** 訪問済みディレクトリのキャッシュ、シンボリックリンクループ検出

## テストケースの追加提案

現在のテストに加えて、以下を追加:

1. **`**`の階層深さテスト**
   ```
   deep/**/**/**/*.txt  # 3階層以上
   ```

2. **絶対パステスト**
   ```
   /tmp/**/*.txt
   ```

3. **混合パステスト**
   ```
   ./**/*.txt  # 相対パス+再帰
   ```

4. **エッジケース**
   ```
   **     # パターンが**のみ
   **/    # ディレクトリのみ
   **/**  # 二重再帰
   ```
